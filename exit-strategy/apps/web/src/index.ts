/** PWA shell (spec §1.5): redeem, play, invite, stake ledger. Next.js
 * lands with T0.4 (redeem flow) — this placeholder keeps the package in
 * typecheck from day one. */
export const WEB_APP_STATUS = 'scaffolded';
